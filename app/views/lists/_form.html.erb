<div class="container">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit 'Add to the list', class: 'btn btn-primary' %>
  <% end %>
</div>
